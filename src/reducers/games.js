export default games(state = [], actions = {}) {
    switch (action.type) {
        case value:
            
            break;
    
        default:
            return state;
            break;
    }
}